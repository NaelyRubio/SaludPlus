<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SaludPlus"
        tools:targetApi="31">

        <!-- Pantalla inicial -->
        <!-- Login y Registro -->
        <activity android:name=".ui.LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.RegisterActivity" />

        <!-- Flujo Paciente -->
        <activity android:name=".ui.PacienteHomeActivity" />
        <activity android:name=".ListaMedicosActivity" />
        <activity android:name=".AgendarCitaActivity" />
        <activity android:name=".ui.MisCitasActivity" />
        <activity android:name=".ui.PerfilPacienteActivity" />
        <activity android:name=".EditarPerfilActivity" />
        <activity android:name=".ui.DetalleCitaPacienteActivity" />

        <!-- Flujo Médico -->
        <activity android:name=".ui.MedicoHomeActivity" />
        <activity android:name=".ui.DisponibilidadActivity" />

        <!-- Generales -->
        <activity android:name=".ui.ListaEspecialidadesActivity" />
        <activity android:name=".utils.NavActivity" />
        <activity android:name=".MainActivity" />


    </application>

</manifest>
